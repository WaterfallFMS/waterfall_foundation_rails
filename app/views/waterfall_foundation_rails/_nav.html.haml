%nav.top-bar{ :data => 'topbar' }
  %ul.title-area
    %li.name
      %h1= link_to page_title, root_path

  - if logged_in?
    %section.top-bar-section
      %ul.right
        %li= link_to 'Logout', logout_path